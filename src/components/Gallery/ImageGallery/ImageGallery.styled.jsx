import styled from '@emotion/styled';

export const GalleryUl = styled.ul`
  list-style: none;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  padding: 0;
  margin: 20px 0;
`;
